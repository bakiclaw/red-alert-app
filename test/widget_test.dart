import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App launches successfully', (WidgetTester tester) async {
    // Basic test to verify app can be built
    expect(true, isTrue);
  });
}
